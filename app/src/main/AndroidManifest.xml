<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.urv.deim.asm.p2.common">
    <!-- This is required for Picasso to work. -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Used to adjust the work load depending on the type of network the device is connected. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Used to load images from the gallery content provider. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="cat.urv.deim.asm.p3.shared.ArticleDetailActivity" />
        <activity
            android:name="cat.urv.deim.asm.p3.shared.FAQsActivity"
            android:label="@string/title_faqs_activity"
            android:screenOrientation="sensor" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile"
            android:screenOrientation="sensor" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:screenOrientation="sensor"
            android:configChanges="orientation|screenSize"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ErrorLoginActivity"
            android:label="@string/title_error_login_activity"
            android:screenOrientation="sensor" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login"
            android:screenOrientation="sensor" />
        <activity
            android:name=".TutorialActivity"
            android:label="@string/title_activity_tutorial"
            android:screenOrientation="sensor"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="sensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>